import { Inter, Prompt,Handlee} from 'next/font/google';
export const inter = Inter({ subsets: ['latin'] });
export const prompt = Prompt({
    weight: ['700','700'],
    subsets: ['latin'],
  });
  export const handlee = Handlee({
    weight: ['400','400'],
    subsets: ['latin'],
  });
  // export const tenorSans = Tenor_Sans({
  //   weight: ['400','400'],
  //   subsets: ['latin'],
  // });